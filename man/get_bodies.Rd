% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_bodies.R
\name{get_bodies}
\alias{get_bodies}
\title{Extract the body table from a system}
\usage{
get_bodies(system)
}
\arguments{
\item{system}{An `orbit_system` object.}
}
\value{
A tibble with columns `id`, `mass`, `x`, `y`, `z`, `vx`, `vy`,
  `vz`.
}
\description{
Returns the bodies in an `orbit_system` as a standalone tibble. Useful
when you want to inspect, filter, or save the body states without
dealing with the full system object.
}
\examples{
sys <- create_system() |>
  add_sun() |>
  add_planet("Earth", parent = "Sun") |>
  add_planet("Mars",  parent = "Sun")

# Get the tibble
get_bodies(sys)

# Use with dplyr
\donttest{
get_bodies(sys) |>
  dplyr::filter(mass > 1e24)
}
}
